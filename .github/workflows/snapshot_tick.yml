name: Snapshot Tick

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

concurrency:
  group: snapshot-tick
  cancel-in-progress: false

jobs:
  tick:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: POST /sugar/snapshot/tick
        run: |
          set -euo pipefail

          status=$(curl -sS \
            -o response.json \
            -w "%{http_code}" \
            -X POST \
            -H "X-Cron-Key: ${{ secrets.SNAPSHOT_CRON_KEY }}" \
            -m 180 \
            https://sugar-cotton-pnl-g9hsasb3bnfjbmat.southeastasia-01.azurewebsites.net/sugar/snapshot/tick)

          echo "HTTP status: $status"
          cat response.json

          test "$status" -ge 200
          test "$status" -lt 300
